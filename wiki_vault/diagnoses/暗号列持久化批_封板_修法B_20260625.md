# 暗号列持久化批封板 — 修法 B (加载项目 annotation 合并优先级反转)

**日期**: 2026-06-25
**状态**: ✅ 封板
**批次**: 暗号列持久化 (修法 B — restore 合并 profile 为底)

## 修复

| 文件 | 行号 | 改动 |
|------|------|------|
| `py/calibration/project_config.py:370-384` | Step 1 合并段 | `merged = dict(file_annot)` → `merged = dict(profile_annot)`，file_annot 仅对 profile 缺失列兜底填充 |

### 改前

```python
merged = dict(file_annot) if file_annot else {}   # 文件为底
for col_name, profile_val in profile_annot.items():
    ...
    elif col_name not in merged or not merged.get(col_name):
        merged[col_name] = profile_val              # profile 补充缺口
```

### 改后

```python
merged = dict(profile_annot) if profile_annot else {} # profile 为底
for col_name, file_val in (file_annot or {}).items():
    ...
    elif col_name not in merged or not merged.get(col_name):
        merged[col_name] = file_val                     # 文件仅兜底
```

## 边界

| 路径 | 是否受影响 | 文件:行号 | 说明 |
|------|-----------|-----------|------|
| 加载项目 | ✅ 本修复覆盖 | `project_config.py:370-384` | restore() Step 1 |
| 新导入文件 | ❌ 不受影响 | `calibration_tab.py:2645-2659` | `_load_and_parse` 直接赋值，不走 restore |
| 同会话编辑后重开 | ✅ 已在孪生批修复 | `calibration_tab.py:1057,1074-1080,1506-1510` | `merge_annotations` + `accept()` 统一落盘 |

## 真机验证

- 加载项目后暗号列恢复：A1-1/A1-2 成对双栅暗号完整，未被文件头旧值覆盖
- `_groups` 识别双栅：合法 12 / 占位 0
- Phase B Ke 表有数据，双栅解耦正常
- 新文件导入路径不受影响（`_load_and_parse` 直接赋值）

## 独立缺陷登记 — 阶段A 手改暗号→重开丢失

**状态**: ✅ 已封板（孪生批）
**封板记录**: `wiki_vault/diagnoses/阶段A手改暗号丢失_封板_20260625.md`
