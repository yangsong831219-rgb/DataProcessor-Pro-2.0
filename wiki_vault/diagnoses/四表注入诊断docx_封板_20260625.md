# 四张数据汇总表 (G1/A1/S2/T5) 注入诊断 docx — 封板

**日期**: 2026-06-25  
**分支**: llama-cpp  
**状态**: ✅ 封板 — 路径A/B 真机均实证通过，三条已知尾巴已登记积压

---

## 根因

`gen_markdown_tables_from_bundle` 唯一调用在路径 A (`main.py:1277` — 报告 workbench)，路径 B (`_build_word_document`) 从不读 `rec["chart_data"]` → 四表数据生成了、写入了 record，但专家组诊断 docx 构建器不读、不渲染、不出表。

溯源详情见 `docs/四表未注入docx溯源.md`。

---

## 修法

**公共提取** — `core/chart_bundle.py`：

| 符号 | 位置 | 职责 |
|------|------|------|
| `TableData` (dataclass) | `chart_bundle.py:425` | 单表结构化数据：`heading`, `headers: list[str]`, `rows: list[list[str]]` |
| `extract_four_tables(cd) -> list[TableData]` | `chart_bundle.py:429` | 从 `chart_data` 字典解析四张 pipe 表格 → 结构化数据。空表/缺表/畸形 → 自动省略 |
| `_parse_single_md_table(md) -> tuple \| None` | `chart_bundle.py:457` | 单表 pipe 格式解析器 |

**路径 A** — `main.py:1275-1301`：
- 改调 `extract_four_tables(cd)` → `_rows_to_md_table` 等价重建 markdown → 原 `WordBuilder` markdown pipeline (`_extract_md_tables` → `_render_md_table_as_word`)

**路径 B** — `ai_diagnosis.py:3077-3091`：
- `_build_word_document` 尾部（页码段之前）→ 读 `rec["chart_data"]` → `extract_four_tables` → `_add_styled_table_docx`（三线表 + 表号表题 + 深蓝表头 + 交替行底色）
- 对 `kind="ai"` 和 `kind="multi"` 均执行

**单测** — `tests/test_chart_bundle_tables.py`：
- `TestParseSingleMdTable`: 6 个用例（正常/对齐分隔行/缺分隔行/表头无数据/空字符串/多余空格）
- `TestExtractFourTables`: 9 个用例（四表齐/部分空/全空/None值/畸形/表头无数据/结构字段/heading 无 markdown 前缀/等价性回归）
- 全部 38 passes

---

## 真机实证

| 文件 | 类型 | 结果 |
|------|------|------|
| `数据分析报告_134609.docx` | 路径 A (报告 workbench) | ✅ G1/T5/S2 三线表注入 |
| `AI诊断_142421.docx` | 路径 B (AI 单诊断) | ✅ "数据汇总附表"段 + 表4/5/6 三表 |
| `多智能体诊断_142627.docx` | 路径 B (专家组诊断) | ✅ "数据汇总附表"段 + 表4/5/6 三表 |

数据齐 (n=6 provider 状态全) 时四表精确注入；AI 单诊断与多智能体均覆盖。

---

## 空表降级行为

- 某表无数据 (e.g. `anomaly_table_md=""`) → `extract_four_tables` 自动省略该表，不崩、不出空壳
- 四表全空 → `if four_tables:` False → "数据汇总附表"段整段静默跳过（无 heading、无表、无日志、无 UI 提示）

---

## 已知尾巴 (已登记 CLAUDE.md 积压，不阻塞封板)

1. **A1 异常表非空验证** — 迄今所有真机数据 `anomaly=False`，仅验了空表降级省略，未验"有异常时正常渲染"
2. **空表 fail-loud** — `ai_diagnosis.py:3082` `if four_tables:` → False → 纯静默。建议加可见提示 "标定数据不可用，跳过数据汇总附表"
3. **[A1]/[B1] 预存调试打印** — `ui/calibration_tab.py:1448,1450,2114,2123,2128,2144` 共 6 处 `print("[A1]..."/"[B1]..."...)` (来自 `7fad573`，非本批)。待清理

---

## 本批确认无遗留临时打印

全仓搜索 `[DIAG.*chart` / `[DIAG.*table` / `[DIAG.*extract` / `[DIAG.*四表` / `[DIAG.*附表` / `print.*chart_data` / `print.*four_table` → **0 命中**。
