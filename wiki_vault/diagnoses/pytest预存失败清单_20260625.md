# pytest 预存失败清单 — 已知可追溯的红

**日期**: 2026-06-25  
**状态**: 14 failed + 1 error, 全部来自前置提交 79b9b10 / 7fad573 / a7b25b0  
**用途**: 对照清单确认"这14个是已知预存, 非新回归"

---

## 当前基线

```
731 passed, 14 failed, 10 skipped, 1 error — pytest tests/ (不含 5 sys.exit(0) 文件 + 3 import 错误文件)
```

---

## 条 1: test_ai_client_backend.py — `test_configure_makes_available`

- **前置提交**: `79b9b10` refactor: migrate to llama.cpp backend
- **根因**: llama.cpp 后端 `is_available()` 逻辑变更 — 本地后端配置后即 True，不再依赖在线 API key
- **断言冲突**: `assert is_available(...)` 期望 False，实际 True
- **文件**: `tests/test_ai_client_backend.py`

---

## 条 2: test_anchored_and_load.py — `test_anchored_chart_plots_working_grating`

- **前置提交**: `a7b25b0` feat(calibration): 应变标定全链路
- **根因**: 锚固模式图表图例文本格式化变更 — `光栅1` 不在新输出中
- **断言冲突**: `assert '光栅1' in '标距: 80.0 mm | tension_only | 1循环...'`
- **文件**: `tests/test_anchored_and_load.py`

---

## 条 3-4: test_data_providers.py — `TestCalibrationProvider` × 2

- **前置提交**: `a7b25b0` feat(calibration): 应变标定全链路
- **根因**: `CalibrationProvider.get_summary` grade 渲染格式变更 — 旧 `"good"` → 新 `"?(未通过)"`，旧 `"FAIL"` → 新 `"?(未通过)"`
- **失败测试**:
  - `test_summary_has_sensors` — 期望 `"1.2"` / `"good"` 在 summary 中
  - `test_summary_FAIL` — 期望 `"FAIL"` 在 summary 中
- **文件**: `tests/test_data_providers.py:180-193`

---

## 条 5: test_diagnosis_save.py — `test_build_record_has_all_fields`

- **前置提交**: `79b9b10` refactor: migrate to llama.cpp backend
- **根因**: schema_version 从 1.1 → 1.2 (`ai_diagnosis.py:1129`)
- **断言冲突**: `assert '1.2' == '1.1'`
- **文件**: `tests/test_diagnosis_save.py`

---

## 条 6: test_phase_a_dialog.py — `test_dirty_column_survives_state_restore`

- **前置提交**: 较早 (具体 commit 待追)
- **根因**: Phase A 对话框状态恢复时 dirty column 被系统默认值覆盖
- **断言冲突**: `dirty column should keep user value, got 'A1-W2'`
- **文件**: `tests/test_phase_a_dialog.py`

---

## 条 7-10: test_phase_b_dialog.py × 4

| 测试 | 前置提交 | 根因 |
|------|---------|------|
| `test_phase_b_result_table` | `a7b25b0` | 补偿流水线新增 `comp_form` + `poly_order` 列 — 测试预期 10 cols, 实际 12 |
| `test_ensure_decoupled_result_lazy_decouple` | `7fad573` | `_ensure_decoupled_result` 逻辑改 — 禁止重算, 仅返回缓存或 None |
| `test_render_result_table_high_hysteresis_shows_fail` | `7fad573` | `_run_compensation_pipeline` 已删除 (CLAUDE.md 已文档化) |
| `test_render_result_table_shows_real_std` | `7fad573` | 同上 |

- **文件**: `tests/test_phase_b_dialog.py`
- **CLAUDE.md 参考**: 铁律第 10 条 "★ 阶段B 任何重计算只在 PhaseBWorker 子线程" / "补偿流水线已静态化"

---

## 条 11-14: test_project_config.py × 4

- **前置提交**: `a7b25b0` feat(calibration): 应变标定全链路
- **根因**: `TEMPERATURE_FIELDS` (`project_config.py:30-35`) 新增 `"compensation"` 字段，但测试 helper `_make_sample_temperature()` (`test_project_config.py:42-56`) 未同步更新
- **失败测试**:
  - `test_save_load_roundtrip_temperature_only` — `compensation` 不在 roundtrip dict 中
  - `test_validate_passes_on_complete_temperature` — `issues == ['temperature 缺少字段: compensation']`
  - `test_save_load_roundtrip_with_strain` — 同上
  - `test_json_on_disk_is_valid` — JSON temperature 缺少 `compensation`
- **文件**: `tests/test_project_config.py:100-239`

---

## 条 15 (ERROR): test_calibration_tab_ui.py — `test_paste_selected_state_multi_row`

- **前置提交**: 较早
- **根因**: 标定页 UI 粘贴多行选择依赖 GUI 环境 / Qt 事件循环
- **报错**: 未完整显示
- **文件**: `tests/test_calibration_tab_ui.py`

---

## 提交根因分布

```
79b9b10 (migrate to llama.cpp):   2 条 (#1, #5)
7fad573 (compensation pipeline):  4 条 (#8-10, 部分 #7)
a7b25b0 (应变标定全链路):          7 条 (#2, #3-4, #7, #11-14)
较早:                              2 条 (#6, #15)
```

---

## 使用方法

```
python -m pytest tests/ -q 2>&1 | grep "failed"
→ 14 failed, 1 error
```

对照本清单逐行确认：若每一条都在清单中 → 无新增回归。若有清单外的 FAILED → 需调查。
