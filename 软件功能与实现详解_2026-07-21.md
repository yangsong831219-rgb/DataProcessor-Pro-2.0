# DataProcessor Pro 2.0 — 软件功能与实现详解

> **生成日期:** 2026-07-21  
> **分支:** llama-cpp  
> **目的:** 全面梳理当前代码现状，为升级规划提供依据  

---

## 目录

1. [项目概览](#1-项目概览)
2. [系统架构总览](#2-系统架构总览)
3. [主入口与窗口框架 (main.py)](#3-主入口与窗口框架)
4. [核心模块 (core/)](#4-核心模块)
5. [数据处理引擎 (dp_engine/)](#5-数据处理引擎)
6. [标定系统 (dp_engine/calibration/)](#6-标定系统)
7. [用户界面 (ui/)](#7-用户界面)
8. [工具模块 (utils/)](#8-工具模块)
9. [状态管理 (state/)](#9-状态管理)
10. [报告构建器 (dp_engine/report_builder/)](#10-报告构建器)
11. [知识管理与多智能体](#11-知识管理与多智能体)
12. [测试体系](#12-测试体系)
13. [已知问题与技术债务](#13-已知问题与技术债务)
14. [升级规划建议](#14-升级规划建议)

---

## 1. 项目概览

**DataProcessor Pro 2.0** 是一款基于 PyQt6 的桌面应用程序，用于光纤光栅 (FBG) 传感器的数据采集、处理、标定和报告生成。

### 核心技术栈

| 层次 | 技术 | 用途 |
|------|------|------|
| UI 框架 | PyQt6 | 桌面 GUI，多标签页架构 |
| 数据处理 | Pandas 2.0+ | DataFrame 操作、数据清洗 |
| 数值计算 | NumPy, SciPy | 科学计算、信号处理 |
| 公式引擎 | asteval | 安全的公式表达式求值 |
| AI 后端 | llama.cpp (OpenAI 兼容 API) | LLM 诊断、报告生成 |
| AI 编排 | LangGraph | 多智能体工作流 |
| 报告生成 | python-docx, python-pptx | Word/PPT 报告渲染 |
| 图表 | matplotlib | 标定图表、诊断图 |
| 数据校验 | Pydantic | Schema 校验、类型安全 |
| 配置持久化 | JSON | 项目配置、标定配置、AI 配置 |
| 测试 | pytest | 单元测试/集成测试 |

### 应用场景

1. **光纤传感器数据采集与分析** — 加载 ENLIGHT/Hyperion/通用 CSV 格式的光纤波长数据
2. **FBG 传感器物理量计算** — 基于波长差和用户自定义公式计算应变、温度等物理量
3. **温度标定 (Phase A + Phase B)** — 温度灵敏度回归 (S_eff) + 双波长解耦 + 表观应变补偿
4. **应变标定** — 手填表驱动的灵敏度、线性度、重复性、迟滞分析
5. **AI 诊断** — 基于本地 LLM 的数据分析与诊断报告
6. **多智能体顾问** — Data Scientist → Advisor → Chief Scientist 三节点审查链
7. **报告生成** — Word 深度技术报告 / PPT 演示汇报

---

## 2. 系统架构总览

### 分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                    UI 层 (ui/)                                │
│  main.py  →  analysis_tab, calibration_tab, compare_tab,     │
│              ai_diagnosis, report_workbench, data_tab, ...    │
├─────────────────────────────────────────────────────────────┤
│                 核心服务层 (core/)                             │
│  ai_client  │ report_engine │ chart_bundle │ data_providers   │
│  chart_registry │ chart_store │ diagnosis_kb │ models         │
├─────────────────────────────────────────────────────────────┤
│                引擎层 (dp_engine/)                             │
│  formula  │ analyzer  │ multi_agent  │ agent_graph            │
│  calibration/  │ report_builder/                              │
├─────────────────────────────────────────────────────────────┤
│                工具层 (utils/)                                 │
│  file_parser │ parse_validation │ annotation_utils             │
│  apparent_strain_comp │ compensation_metrics │ data_cleaning   │
├─────────────────────────────────────────────────────────────┤
│                状态层 (state/)                                 │
│  app_state (SSOT 全局状态)                                     │
└─────────────────────────────────────────────────────────────┘
```

### 关键设计模式

- **SSOT (Single Source of Truth):** `AppState` 作为全局状态的唯一真相源，所有模块通过不可变更新 (`dataclasses.replace`) 进行状态变更
- **单例模式:** `AIClient` 使用单例模式管理 LLM 连接
- **观察者模式:** PyQt6 信号/槽机制实现 UI 组件间解耦通信
- **工厂模式:** `DataProvider` 抽象基类 + 多个具体提供者
- **生产者-注册表模式:** `ChartProducer` 注册到 `chart_registry`，由 `chart_store` 统一调度出图
- **策略模式:** 补偿形式可切换 (`lut` | `poly`)，通过 `comp_form` 参数动态分发

---

## 3. 主入口与窗口框架

### 3.1 main.py — `DataProcessorWindow(QMainWindow)`

**文件规模:** ~2294 行 (已从原始 3240 行拆分)

**主窗口缓存属性 (窗口级状态):**

```python
self.current_data: pd.DataFrame | None        # 当前加载的原始数据
self.current_annotation: dict[str, str]        # 列暗号字典
self.current_columns: list                      # 当前列名列表
self.sampled_data: pd.DataFrame | None          # 降采样数据
self.file_header_lines: list                    # 文件头行
self.sensor_results: dict                       # 传感器计算结果
self.cleaning_rules: list[CleaningRule]         # 数据清洗规则
self.sensor_system: SensorSystem               # 传感器系统管理器
self.state: AppState                            # SSOT 全局状态
self.current_template: DataTemplate | None      # 当前文件模板
```

**标签页 (QTabWidget) 结构 (共 8 个顶级标签页):**

| 索引 | 标签页 | Widget 类 | 功能 |
|------|--------|-----------|------|
| 1 | 数据文件 | `DataTabWidget` | 文件导入、模板选择、预览 |
| 2 | 数据清洗 | `CleaningTabWidget` | 异常检测、缺失值填充 |
| 3 | 光纤公式配置 | `SensorTabWidget` | FBG 编辑、传感器公式定义 |
| 4 | 数据分析 | `AnalysisTabWidget` | 波长/物理量切换、暗号管理、FFT、统计 |
| 5 | 多源对比 | `CompareTabWidget` | 多设备数据对齐、相关性分析 |
| 6 | 传感器标定 | `CalibrationTabWidget` | 温度标定 + 应变标定 |
| 7 | 成果输出与报告 | `QStackedWidget` (6 子页) | 报告生成 + AI 诊断 + 知识管理 |

**第 7 个标签页 (成果输出与报告) 内含 QStackedWidget 的 6 个子页:**

| 子页 | Widget 类 | 功能 |
|------|-----------|------|
| 项目资料管理 | `ProjectManagerWidget` | 项目文件 CRUD |
| 报告生成工作台 | `ReportWorkbenchWidget` | 大纲生成 + 完整报告 |
| AI 诊断 | `AiDiagnosisWidget` | LLM 分析 + 多智能体 |
| 网络剪藏 | `WebClipperWidget` | 网页内容抓取入库 |
| 知识库管理 | `WikiTabWidget` | 本地 Wiki 浏览 |
| 技能插件中心 | `AgentSkillWidget` | Agent 技能市场 |

**关键委托方法 (各 3-5 行壳，实际实现位于 utils/):**

- `_get_analysis_data()` → `utils.annotation_utils.get_analysis_data()`
- `_clean_sensor_keys()` → `utils.column_utils.clean_dict_keys()`
- `_auto_populate_fbgs()` → 正则提取 + `SensorSystem`
- `_insert_annotation_row()` → `utils.annotation_utils.apply_annotation_row()`

### 3.2 数据流

```
文件加载 → parse_file() → DataFrame
    → _auto_populate_fbgs() → FBG 列表
    → _insert_annotation_row() → 暗号行
    → _get_analysis_data() → 分析 DataFrame
    → SensorSystem.calculate() → sensor_results
    → set_sensor_results() → 分析页/传感器页展示
    → AI 诊断 / 标定 / 报告生成
```

---

## 4. 核心模块

### 4.1 AI 客户端 (`core/ai_client.py`, 1124 行)

**类: `AIClient` (单例，`__new__` + `_instance`)**

支持 OpenAI 兼容 API 的 LLM 客户端，支持 online (DeepSeek) 与 local (llama.cpp/Ollama) 双后端切换。配置持久化在 `ai_models_config.json`。

**核心方法:**

| 方法 | 签名 | 功能 |
|------|------|------|
| `configure_online` | `(api_key, base_url, model_name, temperature, max_tokens)` | 在线重配置单例 |
| `generate` | `(prompt, system_prompt, temperature, max_tokens, enable_thinking) -> str` | 同步调用 LLM |
| `generate_stream` | `(prompt, system_prompt, ...) -> Generator[str]` | 流式推理，先缓冲后 yield |
| `generate_with_tools` | `(messages, tools, tool_executable_map, ...) -> str` | 多轮 Function Calling (max 10 轮) |
| `generate_with_retry` | `(prompt, system_prompt, max_retries, base_backoff_s) -> str` | 指数退避重试，仅对 retryable=True 异常 |
| `generate_structured` | `(prompt, schema: BaseModel, system_prompt, ..., max_schema_retries=1) -> Dict` | 结构化输出 + Pydantic 校验 + 回喂修复 |
| `health_check` | `(timeout) -> bool` | 后端可达性探测 |
| `cancel_current_stream` | `() -> None` | 关闭 HTTP 流释放 llama-server slot |
| `is_available` | `() -> bool` | API 是否已配置 |

**关键技术细节:**

- **双后端超时分流:** `LOCAL_READ_TIMEOUT_S=900s` (本地慢模型), `ONLINE_READ_TIMEOUT_S=120s`
- **Token 预算动态钳制:** `max_tokens ≤ ctx − prompt_tokens − margin(256)`，`_safe_max_tokens` 基于 `chars/2 × 1.2` 保守估算
- **Qwen 非思考模式:** 注入 `temperature=0.7, top_p=0.8, top_k=20, presence_penalty=1.5`
- **本地后端:** 显式 `httpx.Client(trust_env=False)` 避免 HTTP_PROXY 拦截 127.0.0.1
- **finish_reason=length:** 自动翻倍 `max_tokens` 重试 1 次 (上限 16384)
- **结构化输出:** JSON 提取 → `model_validate_json()` 校验 → 失败回喂修复 (最多 1 次)
- **流式输出:** 先完整缓冲 tokens 再 yield，避免截断时已输出半截内容
- **JSON 修复:** `_sanitize_json_text` 修复 LLM 输出的非法转义 (LaTeX) 和尾逗号

**异常层级 (`core/ai_errors.py`):**

```
AIClientError (基类)
├── AIClientAuthError (401/403)
├── AIClientRequestError (400/422)
├── AIClientRateLimitError (429, retryable=True)
├── AIClientServerError (5xx, retryable=True)
├── AIClientTimeoutError (连接超时, retryable=True)
├── AIClientEmptyResponseError (200 但 content 为空)
├── AIClientTruncationError (finish_reason=length)
├── AIClientNotConfiguredError (未配置 API)
└── ReportSchemaError (Schema 校验失败, 含 missing_fields/type_errors/raw_text)
```

### 4.2 报告模型 (`core/report_models.py`)

**Pydantic 数据契约:**

```python
WordSection:
    heading: str                          # 章节标题
    paragraphs: List[str]                 # 正文段落
    image_anchors: List[str]              # 图片占位符 (如 "{{fig:时序图}}")
    tables: List[dict]                    # 表格数据

WordReport:
    title: str
    author: str
    date: str
    sections: List[WordSection]

PPTSlide:
    title: str
    bullets: List[str]
    image_anchor: str
    notes: str

PPTReport:
    title: str
    subtitle: str
    slides: List[PPTSlide]
```

### 4.3 报告引擎 (`core/report_engine.py`)

**核心函数:**

| 函数 | 功能 |
|------|------|
| `generate_outline(config, generate_fn)` | 生成报告大纲 (Markdown 格式，人审后继续) |
| `generate_structured_report(outline, report_type, generate_fn)` | 逐节 LLM 生成 → Pydantic 校验 |
| `_build_context(config)` | 构建 AI 上下文 (需求文件 + 项目资料 + 诊断产物) |
| `_build_diagnosis_summary(diag_rec)` | 从诊断记录提取结构化摘要 |

**两阶段工作流:**
1. **大纲生成:** 用户审核 Markdown 大纲 → 人工编辑调整
2. **结构化生成:** 逐节调用 LLM → WordSection Pydantic 校验 → 聚合为 WordReport

### 4.4 图表系统

#### ChartBundle (`core/chart_bundle.py`)

统一的画图数据束 (dataclass)，一一对应 `report_charts` 中各图函数所需输入。

**字段分类:**

| 类别 | 字段 | 用途 |
|------|------|------|
| 时序 | `time_h`, `series`, `cleaned` | 原始波长时序 |
| 物理量 | `phys_series`, `series_delta` | 传感器物理量 Δλ 序列 |
| 对比 | `compare_time_h`, `compare_sources`, `compare_pairs` | 多源对比 |
| 迟滞 | `hyst_sensors` | 迟滞回线 |
| 标定 | `temp_regressions`, `phaseb_diagnostics` | 温度标定图 |
| 标定线性度 | `cal_linearity` | 应变标定线性度 |
| FFT | `fft_spectra` | 频谱分析 |

**构造方式:**
- `ChartBundle.from_providers(main_win)` — 从实时 UI 状态采集
- `ChartBundle.from_diagnosis_record(rec)` — 从已存诊断记录还原

#### ChartRegistry (`core/chart_registry.py`, 503 行)

**`ChartProducer` 数据结构:**
- `chart_id` — 全仓唯一 ID，命名规范 `<module>_<图义>`
- `module` — 归属模块 (data_analysis / strain_calib / compare / phaseb / temperature_calib)
- `tier` — T1=基础数据 / T2=派生物理量
- `required_keys` — ChartBundle 中必须存在的字段
- `produces_when` — 额外条件谓词 (callable)
- `draw_fn` — 延迟绑定的画图函数 (避免循环 import)
- `report_include` — True=进入 Word/PPT；False=仅诊断补充图

**注册的 11 种图表:**

| chart_id | 模块 | tier | report_include | 说明 |
|----------|------|------|----------------|------|
| `strain_calib_lin` | strain_calib | T1 | ✅ | 应变标定线性度图 |
| `data_ts_cleaning` | data_analysis | T1 | ✅ | 数据清洗前后对比 |
| `data_ts_dlambda` | data_analysis | T2 | ✅ | 波长漂移时序 |
| `data_ts_strain` | data_analysis | T2 | ✅ | 应变物理量时序 |
| `data_ts_temperature` | data_analysis | T2 | ✅ | 温度物理量时序 |
| `data_ts_formula` | data_analysis | T2 | ✅ | 公式计算量时序 |
| `tempa_regression` | temperature_calib | T1 | ✅ | 温度灵敏度回归网格 |
| `phaseb_diagnostic` | temperature_calib | T1 | ✅ | Phase B 诊断四联图 |
| `compare_ol` | compare | T1 | ✅ | 多源对比叠加图 |
| `compare_corr_scatter` | compare | T1 | ✅ | 相关性散点图 |
| `phaseb_hyst` | phaseb | T1 | ❌ | 迟滞回线 (诊断补充图) |

#### ChartStore (`core/chart_store.py`)

**`build_chart_store(chart_data, charts_dir, warnings)`** — 核心调度函数:
1. 遍历所有注册的 `ChartProducer`
2. 检查 `required_keys` 是否满足 → 不满足则 skip
3. 检查 `produces_when` 条件
4. 调用 `draw_fn` 产 PNG
5. 落盘到 `charts_dir`
6. 返回 `ChartManifestEntry` 列表

### 4.5 数据提供者 (`core/data_providers.py`, 961 行)

**抽象基类 `DataProvider`:**

```python
class DataProvider(ABC):
    display_name: str       # UI 复选框显示名
    disabled_hint: str      # 禁用时的提示文案

    @abstractmethod
    def is_available(self, main_win) -> bool: ...
    @abstractmethod
    def get_summary(self, main_win, budget_chars=2000) -> str: ...
```

**6 个具体 Provider 实例:**

| Provider | display_name | is_available 判据 | get_summary 关键输出 |
|----------|-------------|-------------------|---------------------|
| `DataFileProvider` | 数据文件 | `current_data` 非空 | 模板/类型/规模/时间范围/采样间隔/列→传感器映射/数值列统计 |
| `CleaningProvider` | 数据清洗 | `_cleaning_has_run=True` | 异常总数/异常列明细/清洗策略 |
| `AnalysisProvider` | 数据分析 | `_current_data` 非空 | 逐列统计 (均值/sigma/范围/漂移/稳定性) |
| `CompareProvider` | 多源对比 | `_last_comparison` 非空 | 对齐方法/成对对比 (corr/MAE/RMSE/max_dev) |
| `CalibrationProvider` | 传感器标定 | Phase A 或 B 有结果 | KT/Ke1/Ke2/e_std/补偿σ/迟滞/评级 |
| `ExternalDataProvider` | 外部数据文件 | `_external_files` 非空 | 文件内容预览 (CSV/Excel/文本) |

**关键工具函数:**
- `compute_time_series_features(series) -> dict` — 纯函数，计算 n/mean/std/min/max/range/drift_slope_per_1k/stability
- `_get_calibration_annotation()` / `_get_strain_ke()` — 从标定页取列注解/应变 Ke

### 4.6 诊断知识库 (`core/diagnosis_kb.py`, 444 行)

**基于 YAML 规则引擎的 FBG 诊断知识库。**

**核心类:**

| 类 | 说明 |
|----|------|
| `KBRule` | 规则实体: id/domain/scope/trigger(Python表达式)/severity/meaning/mechanism/recommendation |
| `DiagnosisKB` (单例) | 知识库管理器: YAML加载→安全求值→条件触发→RAG组装 |

**安全求值引擎 (`evaluate_trigger`):**
- 白名单内置函数: `any/all/abs/max/min/len/median/list`
- `__builtins__ = {}` (完全阻断系统函数)
- AST 遍历禁止 `import/exec/__` 前缀属性
- `constants.X` 通过 `_AttrDict` 属性访问
- 任意异常 → False (防御性)

**检索流程:**
```
UI 指标数据 → DiagnosisKB.retrieve(sensor_data, multisource_data, cleaning_data)
  → 按 scope 分组求值 (sensor/multisource/cleaning/global)
  → 收集命中规则 (去重，按 severity 排序)
  → assemble_rag_prompt(hits, budget_chars=3000)
  → 注入 AI 诊断 prompt
```

**规则作用域:**
- `scope="sensor"` — 单传感器指标触发 (σ/滞回/噪声底/灵敏度)
- `scope="multisource"` — 多源对比偏差触发
- `scope="cleaning"` — 数据质量触发
- `scope="global"` — 全局条件触发 (如全传感器 FAIL)

### 4.7 核心模型 (`core/models.py`)

| 类 | 功能 |
|-----|------|
| `FBG` | 光纤光栅定义 (id, channel, wavelength_min, wavelength_max) |
| `Sensor` | 传感器定义 (id, type, formula, constants, decoupling_config, location) |
| `SensorSystem` | 传感器系统管理器 — 管理 FBG 列表 + Sensor 列表，执行物理量计算 |
| `DataTemplate` | 数据文件模板 (分隔符、跳过行、列定义) |
| `CleaningRule` | 数据清洗规则 |
| `GlobalParameter` | 全局参数 (name, value, unit, description) |

---

## 5. 数据处理引擎

### 5.1 公式引擎 (`dp_engine/formula.py`)

**核心: `FormulaEngine` 类 + `asteval.Interpreter`**

```python
class FormulaEngine:
    def set_param(name, value)     # 设置参数
    def set_column(name, values)   # 设置列数据
    def evaluate(expression)       # 安全求值
```

**便捷函数: `calculate(formula, columns, params, global_params)`**

- 全局参数打底 → 局部常量覆盖 (优先级: 局部 > 全局)
- 每次调用重建独立引擎实例，防止列数据跨调用泄漏
- 支持向量化计算 (NumPy 数组)
- 彻底消除 k1/k10 误匹配风险 (参数名直接作为 symtable 键)

### 5.2 信号分析器 (`dp_engine/analyzer.py`)

**类: `SignalAnalyzer`**

- FFT 频谱分析
- 峰值检测
- 噪声底计算 (基于 `rolling().median()`, 25× 性能提升)
- 信号质量评估

### 5.3 处理器 (`dp_engine/processor.py`)

数据处理器，封装常规的数据转换和格式化操作。

---

## 6. 标定系统

### 6.1 温度标定 (`dp_engine/calibration/temperature_calibration.py`)

**双阶段标定流程:**

#### Phase A — 温度灵敏度回归 (S_eff)

```
温度台阶实验数据 → 平台检测 (KMeans 聚类)
    → 提取各温度台阶的稳定段波长
    → 线性回归: Δλ vs ΔT → S_eff (pm/°C)
    → R² 评估拟合质量
```

关键函数:
- `detect_plateaus(T, min_duration, ...)` — KMeans 平台检测
- `compute_dL(df, wavelength_cols)` — 计算波长漂移 (Δλ = λ_current − λ_base)
- `regress_S_eff(dL, T, ...)` — 线性回归求温度灵敏度

#### Phase B — 双波长解耦 + 补偿

```
Phase A 结果 (S_eff) + 用户 Ke (应变灵敏度)
    → decouple(dl1, dl2, Ke1, KT1, Ke2, KT2) — 矩阵求逆解耦
    → 应变时序 ε(t), 温度时序 T(t)
    → 表观应变补偿 (LUT 或多项式)
    → 指标评估 (σ_ε, 滞回, 噪声底)
    → 传感器评级 (优/良/合格/FAIL/N/A)
```

**解耦公式:**
```
| dL1 |   | Ke1  KT1 | | ε  |
| dL2 | = | Ke2  KT2 | | dT |

ε  = (KT2·dL1 − KT1·dL2) / det
dT = (Ke1·dL2 − Ke2·dL1) / det
det = Ke1·KT2 − KT1·Ke2
```

**PhaseBWorker (QThread):**
- 在子线程内执行解耦 + 补偿全流程
- 支持 `comp_form` = "lut" | "poly", `poly_order` = 2|3|4
- 支持 `subsample_step` 抽样 (LOOCV 建模用)
- 单栅传感器: 汇总温度系数，不做解耦

### 6.2 应变标定 (`dp_engine/calibration/strain_calibration.py`)

**基于手填表的应变标定流程:**

```
用户加载应变标定数据文件
    → 指派 FBG 通道到传感器
    → 填入各级应变加载值
    → 自动计算: 灵敏度 (pm/με), 线性度 (%FS), 重复性, 迟滞
    → 生成指标图表
    → 支持应用标定系数到传感器 (会话级)
```

**关键指标:**
- 灵敏度 (sensitivity): 线性回归斜率
- 线性度 (linearity): 最大残差 / 满量程
- 重复性 (repeatability): 同级多次测量标准差
- 迟滞 (hysteresis): 正反行程最大差值

### 6.3 标定配置管理 (`dp_engine/calibration/project_config.py`)

**`ProjectConfig`:**
```python
class ProjectConfig:
    name: str
    created_at: str
    last_modified: str
    temperature: dict | None    # 温度标定全部内容
    strain: dict[str, StrainSubConfig]  # 传感器名 → 应变配置
```

**`ProjectConfigManager`:**
- `capture(temperature_page, strain_page)` — 快照当前双模块状态
- `restore(pc, temperature_page, strain_page)` — 回填双模块活状态
- `save(pc)` / `load(name)` — 持久化到磁盘

### 6.4 标定曲线配置 (`dp_engine/calibration/profile.py`)

`CalibrationProfile` — 标定流程参数预置 (温度范围、台阶数、采样间隔等)。

### 6.5 读数管理 (`dp_engine/calibration/readings_profile.py`)

`ReadingsProfile` — 管理应变标定的读数记录 (手填表数据)。

### 6.6 台阶提取器 (`dp_engine/calibration/step_extractor.py`)

从温度台阶实验数据中自动提取各台阶的稳定段。

### 6.7 导出工具 (`dp_engine/calibration/export_utils.py`)

标定结果导出为 Excel 格式。

---

## 7. 用户界面

### 7.1 分析标签页 (`ui/analysis_tab.py`)

**功能:**
- 原始数据/物理量数据切换显示
- 光纤数据 (is_fiber_data): 基于波长差，允许用户自由切换
- 通用数据 (TXT/CSV): 锁定在"物理量"模式，直接读取注释列
- 暗号行显示与编辑
- FFT 频谱分析触发

**关键约束 (硬纪律):**
- 严格的 IF-ELSE 物理隔离: 光纤数据 vs 非光纤数据走不同逻辑分支
- 强制使用 `self.window()` 而非 `self.parent()` 获取主窗口属性

### 7.2 标定标签页 (`ui/calibration_tab.py`)

**文件规模:** ~4316 行 (最大 UI 文件)

**子页结构:**
- **温度标定子页:** 文件加载 → 列角色指派 → 设定温度 → Phase A (S_eff 回归) → Phase B (解耦+补偿) → 诊断图 → Word 报告
- **应变标定子页:** 文件加载 → 传感器列表 → 动态填表 → 自动计算 → 指标图表 → 系数闭环

**关键类:**
- `CalibrationTab(QWidget)` — 主容器 (189 个符号)
- `PhaseBDialog(QDialog)` — Phase B Ke 输入 + 诊断展示
- `PhaseBWorker(QThread)` — 子线程解耦计算
- `ComputeFormsCompareWorker(QThread)` — 补偿形式对比计算

**关键规则 (硬纪律):**
- Phase B 任何重计算只在 `PhaseBWorker` 子线程
- 按钮异步模式: `_last_result` 有缓存 → 直接续；无缓存 → 启动 worker
- `compare_forms` 按需单传感器惰性算
- 对话框写回必须覆盖全部关闭路径 (accept + reject + Esc + X)

### 7.3 多源对比标签页 (`ui/compare_tab.py`)

**功能:**
- 加载多个设备的同类型数据文件
- 时间对齐与重采样
- 相关性分析 (corr, RMSE, MAE)
- 对比图表生成
- 设备间偏差诊断

### 7.4 AI 诊断 (`ui/ai_diagnosis.py`)

**类: `AiDiagnosisWidget`**

**双诊断模式:**
1. **AI 单次诊断:** 勾选数据源 → 构建上下文 → LLM 分析 → 结构化 JSON 结果
2. **多智能体诊断:** Data Scientist → Advisor → Chief Scientist 三节点

**UI 布局:** 40:60 双栏仪表盘
- 左侧: 数据源复选框 + 模型选择 + 操作按钮
- 右侧: Markdown 结果展示 + 结构化数据视图

**诊断记录:** `_diagnosis_record` dict 供报告引擎消费

### 7.5 报告工作台 (`ui/report_workbench.py`)

**类: `ReportWorkbenchWidget`**

**信号:**
- `outline_requested(dict)` — 请求生成大纲
- `full_report_requested(dict, str)` — 请求生成完整报告 (配置 + 大纲)
- `load_diagnosis_requested()` — 加载已存诊断
- `cancel_requested()` — 取消生成

**工作流:**
```
配置报告参数 → 生成大纲 (Markdown) → 人审编辑 → 生成完整报告 → 渲染 docx/pptx
```

### 7.6 后台报告工作线程 (`ui/report_worker.py`)

**类: `ReportWorker(QThread)`**

通用后台工作线程，在子线程执行 AI/渲染任务:
- `progress` 信号 — 进度更新
- `finished` 信号 — 任务完成
- `error` 信号 — 错误信息

### 7.7 对话框

| 对话框 | 文件 | 功能 |
|--------|------|------|
| `FbgEditDialog` | `fbg_edit_dialog.py` | FBG 编辑/添加 |
| `SensorEditDialog` | `sensor_edit_dialog.py` | 传感器编辑/添加 (含解耦矩阵面板) |
| `AiModelConfigDialog` | `ai_model_config_dialog.py` | AI 模型 API 配置 |
| `GlobalParameterDialog` | `global_parameter_dialog.py` | 全局参数编辑 ("笨组件"，纯信号驱动) |

### 7.8 图表组件 (`ui/widgets/`)

- `ChartPanel` — 嵌入式图表面板 (matplotlib 嵌入 PyQt)
- `ChartsDialog` — 弹出式图表查看器

### 7.9 公共组件 (`ui/components.py`)

可复用的 UI 工厂函数 (`create_button`, `create_section_header` 等)。

### 7.10 其他标签页

| 标签页 | 文件 | 功能 |
|--------|------|------|
| 数据加载 | `data_tab.py` | 文件导入、模板管理 |
| 数据清洗 | `cleaning_tab.py` | 异常检测、缺失值填充 |
| 传感器管理 | `sensor_tab.py` | FBG/Sensor 列表管理 |
| 项目 | `project_tab.py` | 项目文件管理 |
| 技能 | `skill_tab.py` | Agent 技能展示 |
| Wiki | `wiki_tab.py` | 知识库浏览 |
| 裁剪 | `clipper_tab.py` | 网页剪藏 |

---

## 8. 工具模块

### 8.1 文件解析 (`utils/file_parser.py`, 1208 行)

**核心函数:**

| 函数 | 功能 |
|------|------|
| `detect_format(file_path, encoding, template_skip_rows)` | csv.Sniffer + 手工检测最佳分隔符和数据起始行 |
| `detect_header(file_path, delimiter, skip_rows, encoding)` | 智能检测表头行 (数值比例≥50%→数据行) |
| `try_read_csv(file_path, delimiter, skip_rows, encoding, header)` | 多编码自动回退读取 (utf-8→gbk→latin-1) |
| `parse_file(file_path, template)` | 统一解析入口，按 file_format 分发 |
| `parse_enlight_file(path)` | ENLIGHT 内容感知三分支入口 |
| `parse_enlight_sensors(path)` | Hyperion Sensors 格式解析 (Timestamp\t表头→FBG_列→6项硬断言) |
| `parse_hyperion_peaks(lines, header_idx)` | Hyperion Peaks 计数格式解析 (16通道峰值→波长槽位对齐) |
| `auto_detect_template(file_path)` | 自动检测 Peaks/Sensors/通用CSV/光纤自定义 |
| `load_custom_templates / persist_custom_template` | 自定义模板 JSON 持久化 |
| `detect_numeric_wavelength_columns(df)` | 数值列识别 + 波长列识别 (均值 1500-1600nm) |
| `_looks_like_annotation_row(cells)` | 暗号行判定 (引号包裹非数值单元 + 无裸数值) |

**格式三分支解析流程:**

```
文件加载 → 内容感知探查 (BOM/元数据标志/Timestamp\t表头)
  ├── Hyperion Peaks:    # CH 计数列 → _find_peaks_header → parse_hyperion_peaks
  │                       (计数列→槽位对齐，峰丢失填 NaN)
  ├── Hyperion Sensors:  Timestamp\t 表头 → parse_enlight_sensors
  │                       (暗号行检测→逐行解析→FBG_列识别→6项硬断言)
  └── Legacy ENLIGHT:    扫描连续时间戳行定位数据起始 → pd.read_csv fallback
→ _run_parse_validation(strict) → validate_parsed_data → (df, annotation, meta)
```

**关键实现细节:**
- `parse_hyperion_peaks`: 第一遍扫描全局最大每通道峰数确定槽位布局，第二遍对齐填充
- Sensors 硬断言 (6项): Timestamp 列存在/有数据行/文件大但数据少→表头定位错误/元数据泄漏至列名/FBG_列数一致性
- `auto_detect_template`: Peaks(`# CH` 计数列) → Sensors(`Timestamp\t` 无 `# CH`) → 通用(分隔符检测+is_data_row判断+光纤列名推断)
- 自定义模板持久化: `custom_templates.json`，加载时按 ID 去重

### 8.2 解析校验 (`utils/parse_validation.py`)

**`validate_parsed_data(df, meta, source_path, strict)`**

- **strict 模式 (ENLIGHT/Hyperion):** 校验 Timestamp 列、FBG 波长区间 1400-1700nm、数值纯度、元数据泄漏
- **lenient 模式 (通用):** 仅做空表/行数/元数据泄漏基础校验
- 失败抛 `ParseValidationError`

### 8.3 暗号系统 (`utils/annotation_utils.py`)

**暗号 (Annotation) 机制:** 在数据 DataFrame 中插入特殊标记行，记录每列的物理含义和单位。

| 函数 | 功能 |
|------|------|
| `build_annotation_row(col_roles)` | 构建暗号行 dict |
| `insert_blank_row(df)` | 在 DataFrame 顶部插入空行 |
| `apply_annotation_row(df, annotation)` | 将暗号字典应用到 DataFrame |
| `extract_annotation_info(df, template, sensor_system)` | 从 DataFrame 扫描暗号，解析列角色 |
| `get_analysis_data(df, annotation_info, ...)` | 提取去除暗号行后的分析数据 |
| `build_sensor_results_dict(...)` | 构建传感器结果字典 |
| `is_wave_col(col_name)` | 判断列名是否为波长列 (1400-1700nm) |

### 8.4 列名工具 (`utils/column_utils.py`)

| 函数 | 功能 |
|------|------|
| `strip_bracket_units(name)` | "卸妆"正则 — 剥离中英文括号及单位 |
| `clean_dict_keys(d, pattern)` | 批量清理字典键 |
| `is_data_row(row)` | 判断 DataFrame 行是否为数据行 |
| `detect_fbg_columns(df)` | 自动检测 FBG 波长列 |

### 8.5 表观应变补偿 (`utils/apparent_strain_comp.py`)

**核心数据结构:**

```python
@dataclass
class ApparentStrainLUT:
    T_grid: list[float]      # 温度网格 (°C)
    eps_app: list[float]     # 表观应变 (με)
    T_base: float            # 参考温度
    ...

@dataclass
class CompensationModel:
    form: str                # "lut" | "poly"
    T_min: float
    T_max: float
    lut: ApparentStrainLUT | None
    poly_coeffs: list[float] | None
    poly_order: int
    ...
```

**核心函数:**

| 函数 | 功能 |
|------|------|
| `build_apparent_strain_lut(T, eps, T_base, ...)` | 从 (T, ε) 时序构建 LUT 补偿表 |
| `apply_apparent_strain_comp(eps_dec, T, lut, ...)` | 查表补偿 (越界钳位) |
| `build_poly_model(T, eps, T_base, order, ...)` | 构建多项式补偿模型 |
| `apply_compensation_model(eps_dec, T, cm, ...)` | 统一补偿应用 (LUT or poly) |

**越界行为 (硬纪律):**
- T < T_min → 钳位到端点值
- T > T_max → 钳位到端点值
- 多项式严禁外推 (4 阶在标定区外发散极快)
- `oob_mask[i] = True` 标记越界点，调用方必须检查并弹 UI 告警

### 8.6 补偿指标 (`utils/compensation_metrics.py`, 1000 行)

**核心数据结构:**

| 类 | 字段 | 说明 |
|----|------|------|
| `GradeThresholds` | `thr_hysteresis_fail_pct_fs(5.0)`, `thr_sigma_excellent(1.0)`, `thr_sigma_good(2.0)`, `thr_sigma_pass(4.0)`, `thr_worst_case_excellent(1.5)` | 五级评级阈值 (SSOT) |
| `CompensationMetrics` | `residual_sigma`, `repeatability`, `hysteresis_max`, `noise_floor`, `temp_sensitivity_max`, `worst_case_single`, `low_confidence`, 所有 %FS 版本 | 补偿后六大指标 |
| `SensorGrade` | `sensor`, `grade`, `passed`, `reasons[]`, `is_single_grating` | 传感器出厂评级 |

**六大指标计算方法:**

| 指标 | 计算方法 |
|------|---------|
| `residual_sigma` | ≥3循环→LOOCV (留一循环建模型→在留出循环上算 std); <3循环→in-sample |
| `repeatability` | 15箱分箱，每箱内各循环均值间 std，取最大箱 (pandas groupby 向量化) |
| `hysteresis_max` | 排除首循环(瞬态)，强平滑找峰值→升/降支→1.5°C分箱→每bin |mean(up)−mean(down)|→取max |
| `noise_floor` | eps − rolling median = residual → std (pandas 向量化, 25× 性能提升) |
| `temp_sensitivity_max` | 从 CompensationModel 计算 max|deps_app/dT| |
| `worst_case_single` | 所有校后点最大绝对偏差 (%FS) |

**评级逻辑 (硬纪律):**

```
grade_sensor(metrics, thresholds):
  1. hysteresis_max > 5%FS → FAIL (废品拦截线)
  2. hysteresis_max = NaN (未测升降支) → low_confidence=True, 上限"良"
  3. sigma ≤ 1%FS → "优" (额外门槛: worst_case_single ≤ 1.5%FS, 否则降"良")
  4. sigma ≤ 2%FS → "良"
  5. sigma ≤ 4%FS → "合格"
  6. sigma > 4%FS → "不合格"
  7. single_grating=True → "N/A" (不参与补偿)
  8. 异常 → "ERROR" (严禁与 N/A 混淆)
```

**关键函数:**

| 函数 | 功能 |
|------|------|
| `evaluate_compensation(T, eps, cids, fs, form, poly_order, ...)` | LOOCV 主入口: 六大指标全量计算 |
| `grade_sensor(metrics, thresholds)` | 传感器评级 |
| `grade_sensor_na(name, reason)` | 单栅传感器 N/A 评级 |
| `compare_compensation_forms(T, eps, cids, fs, candidates, ...)` | 多形式对比 (LUT vs poly2 vs poly4) |
| `detect_cycles_from_T(T, method)` | 温度循环自动检测 (uniform_filter1d + find_peaks) |

**性能优化:**
- `subsample_step` 抽样: LOOCV/建模用抽样数据，统计量始终用全量数据
- 实测 9.8万行: noise_floor 887ms→35ms (25×), compare_forms 2798ms→194ms (14×), LOOCV 残差偏差 <2%

### 8.7 数据清洗 (`utils/data_cleaning.py`)

- `clean_data(df, rules)` — 按规则清洗数据
- `detect_anomalies(df, method)` — 异常检测
- `fill_missing(df, method)` — 缺失值填充

### 8.8 DataFrame 工具 (`utils/dataframe_utils.py`)

- `find_first_timestamp_row(df)` — 定位第一个时间戳行

---

## 9. 状态管理

### 9.1 AppState (`state/app_state.py`)

**SSOT 全局状态容器 (dataclass):**

```python
@dataclass
class AppState:
    version: str = '2.0'
    fbgs: List[FBG]
    sensors: List[Sensor]
    project_files: List[str]
    global_parameters: Dict[str, GlobalParameter]
    data_tab: DataTabState
    cleaning_tab: CleaningTabState
    analysis_tab: AnalysisTabState

    # 不可变更新方法
    def with_fbgs(self, fbgs) -> 'AppState': ...
    def with_sensors(self, sensors) -> 'AppState': ...
    def with_global_parameters(self, params) -> 'AppState': ...
    def with_data_file(self, path, df) -> 'AppState': ...
    def with_analysis_results(self, results) -> 'AppState': ...

    # 序列化
    def to_dict(self) -> Dict: ...
    @classmethod
    def from_dict(cls, d) -> 'AppState': ...
```

**子状态:**

- `DataTabState` — 文件路径、DataFrame 形状、模板信息
- `CleaningTabState` — 清洗规则配置
- `AnalysisTabState` — 暗号信息、传感器结果引用

---

## 10. 报告构建器

### 10.1 Word 构建器 (`dp_engine/report_builder/word_builder.py`)

**类: `WordReportBuilder` (53 个符号)**

**功能:**
- 基于模板 (`.docx`) 或空文档构建 Word 报告
- 渲染章节标题、正文段落、表格
- 图片锚点替换 (`{{fig:xxx}}` → 实际图片)
- 公式渲染 (LaTeX → 图片或 MathML)
- 样式继承 (从模板继承字体、颜色、页眉页脚)

**关键方法:**
- `build(report_data)` — 主渲染入口
- `_add_section(section)` — 渲染单个章节
- `_add_table(table_data)` — 渲染表格
- `_inject_figures(section, figures_dir)` — 图片注入
- `_render_formula(formula_text)` — 公式渲染

### 10.2 PPT 构建器 (`dp_engine/report_builder/ppt_builder.py`)

**类: `PPTBuilder` (28 个符号)**

**功能:**
- 基于模板 (`.pptx`) 或空演示文稿构建 PPT
- 渲染幻灯片标题、项目符号、图片
- 演讲者备注
- `build_ppt_report()` — 报告主入口 (含空模板 fallback)

### 10.3 模板引擎 (`dp_engine/report_builder/template_engine.py`)

**类: `TemplateEngine`**

- 模板变量替换 (`{{var}}` 语法)
- 条件渲染
- 循环渲染 (表格行)

### 10.4 流程控制器 (`dp_engine/report_builder/flow_controller.py`)

**类: `ReportFlowController`**

- `generate_outline()` / `expand_section()` — 当前无外部调用者 (死代码)
- 若被调用会裸抛异常

### 10.5 构建器模型 (`dp_engine/report_builder/models.py`)

与 `core/report_models.py` 平行的数据模型 (非 Pydantic):
- `WordReport`, `WordSection`, `WordTable`
- `PPTReport`, `PPTSlide`
- 含有 `to_dict()` / `from_dict()` 序列化方法

---

## 11. 知识管理与多智能体

### 11.1 多智能体系统 (`dp_engine/multi_agent.py`, 681 行)

**三节点线性工作流 (LangGraph StateGraph):**

```
START → Data Scientist (详报) → Advisor (审查意见) → Chief Scientist (精炼综合→JSON) → END
```

**各节点职责与输入预算:**

| 节点 | 输入来源 | 输入预算 | 输出 |
|------|---------|---------|------|
| Data Scientist | 用户指令 + 数据上下文 | 动态 `_safe_max_tokens` | 详报 (完整) |
| Advisor | DS 详报 (裁剪) | ~2000 token | 审查意见 (自由文本) |
| Chief Scientist | DS 摘要 (~800 token) + 审查意见 (~400 token) | 动态计算 | 综合 JSON (Schema 约束) |

**关键设计特性:**
- **输入预算安全:** `_safe_max_tokens` = `ctx − prompt_est − margin(600)`，下限 512
- **截断降级:** Chief 触发 `AIClientTruncationError` → 降级保底，返回 `partial_content`
- **超时诊断:** `AIClientTimeoutError` → 给出含预估时间的明确诊断，不自动重试
- **数据防火墙:** 通过 `build_context_block` 注入 `is_fiber` 区分光纤/通用路径
- **Phase 6 线性单遍:** 不再有条件边/驳回重跑循环 (已简化)
- **Advisor 极度宽松:** 空响应 → 空字符串不抛异常

**Chief Scientist 输出 JSON Schema:**

```json
{
  "diagnosis_summary": "综合诊断摘要 (2-4句)",
  "data_quality": {"score": "优良中差", "issues": ["..."], "recommendations": ["..."]},
  "sensor_analysis": [{"sensor": "...", "status": "正常/异常", "details": "..."}],
  "physical_diagnosis": {"assessment": "...", "confidence": "高/中/低"}
}
```

**Qt 线程封装:**
- `MultiAgentThread(QThread)` — 子线程调用 `run_multi_agent`
- 信号: `progress(str)` / `finished(dict)` / `error(str)`

### 11.2 Agent 图谱 (`dp_engine/agent_graph.py`)

**独立的 Agent 工作流 (不同于 multi_agent):**

```
START → llm_brain → (条件边) → action_tools → llm_brain → END
```

- `llm_brain` — LLM 推理节点
- `action_tools` — 工具执行节点
- `should_continue` — 条件路由 (call_tools vs end)

### 11.3 Agent 技能中心 (`dp_engine/agent_skill_hub.py`)

管理和分发 Agent 技能定义。

### 11.4 Agent Worker (`dp_engine/agent_worker.py`)

`DeepSeekAgentWorker` — 当前无 Python 调用者 (仅文档引用)。

### 11.5 Wiki 系统 (`dp_engine/wiki_system.py`)

本地知识库管理系统:
- Markdown 文件的 CRUD
- 全文搜索
- 分类标签
- 版本历史

### 11.6 网页剪藏 (`dp_engine/web_clipper.py`)

**类: `WebClipper`**

一站式剪藏流程:
```
抓取 URL → HTML → Markdown → 图片本地化 → 内容清洗 → 入库 Wiki
```

### 11.7 GitHub 技能加载器 (已移除)

原 `dp_engine/github_skill_loader.py` 已于 2026-07-21 删除（第一批 1.2 安全封板）。
该模块包含 `importlib.util.spec_from_file_location` 动态代码加载，存在安全风险。

替换方案:
- 来源检查: `dp_engine/github_skill_source.py::inspect_github_skill_source()`
- 技能安装: SkillInstaller (第二批实现)
- 技能执行: SkillRuntime (第二批实现)

---

## 12. 测试体系

### 12.1 测试统计

- **测试文件:** 80+ 个 `test_*.py` 文件
- **测试类型:** 单元测试 + 集成测试 + Smoke 测试
- **测试框架:** pytest

### 12.2 关键测试领域

| 领域 | 测试文件 | 测试数量 |
|------|----------|----------|
| AI 客户端 | `test_ai_client.py`, `test_ai_client_backend.py`, `test_ai_stream_thread.py` | 126 + 98 + 34 |
| 报告图表 | `test_report_charts.py` | 130 |
| 补偿指标 | `test_compensation_metrics.py` | 95 |
| 应变读数 | `test_strain_readings.py` | 84 |
| 表观应变补偿 | `test_apparent_strain_comp.py` | 77 |
| 图表 Bundle | `test_chart_bundle_tables.py` | 78 |
| 图表注册表 | `test_chart_registry_store.py` | 43 |
| Phase B 守卫 | `test_phase_b_guard_regression.py` | 140 |
| 多结果 SSOT | `test_multi_result_ssot.py` | 120 |
| 数据提供者 | `test_data_providers.py` | 68 |
| 标定数学 | `test_calibration_math.py` | 59 |
| 公式引擎 | `test_formula_engine.py` | 25 |
| 文件解析 | `test_enlight_parser.py`, `test_parse_*.py` | 多项 |

### 12.3 已知预存失败

**14f + 1e** — 全部来自前置提交:
- `79b9b10` (refactor: migrate to llama.cpp backend)
- `7fad573` (部分功能未完成)
- `a7b25b0`

每个 pytest 跑完需对照 `wiki_vault/diagnoses/pytest预存失败清单_20260625.md` 确认无新增回归。

---

## 13. 已知问题与技术债务

### 13.1 框架级

| ID | 问题 | 严重程度 | 位置 |
|----|------|----------|------|
| DT-1 | `main.py` Phase 2 Mixin 拆分缓做 | 低 | main.py (2294 行) |
| DT-2 | `py/` → `dp_engine/` 目录改名完成，pytest 冲突已根治 | ✅ 已修复 | — |
| DT-3 | `except: pass` 静默吞异常 | 禁止 | 全仓 |

### 13.2 报告系统

| ID | 问题 | 严重程度 | 位置 |
|----|------|----------|------|
| RP-1 | A1 异常表非空验证未真机验 | 低 | `word_builder.py` |
| RP-2 | 四表全空静默跳过 (fail-loud 缺失) | 低 | `ai_diagnosis.py:3082` |
| RP-3 | PPT 模板缺失时 `Presentation('')` 直接抛错 | 中 | `ppt_builder.py:274` |
| RP-4 | 报告 prompt 领域约束 (甲-1) — 经真机实证已砍掉 | ✅ 不需要 | — |

### 13.3 AI / LLM

| ID | 问题 | 严重程度 | 位置 |
|----|------|----------|------|
| AI-1 | `ConnectError` 被误分类为 `AIClientTimeoutError` | 中 | `ai_errors.py:190` |
| AI-2 | `AIClient.generate_structured()` 可切原生 `response_format=json_schema` | 低 | `ai_client.py` |
| AI-3 | `DeepSeekAgentWorker` 无 Python 调用者 | 低 | `agent_worker.py` |

### 13.4 标定系统

| ID | 问题 | 严重程度 | 位置 |
|----|------|----------|------|
| CL-1 | `[A1]/[B1]` 等 6 处预存 `print()` 调试打印 | 低 | `calibration_tab.py` |
| CL-2 | 中段第二条暗号行 `_looks_like_annotation_row` 漏检 | 低 | `file_parser.py` |

### 13.5 流程控制

| ID | 问题 | 严重程度 | 位置 |
|----|------|----------|------|
| FC-1 | `ReportFlowController.generate_outline()` / `expand_section()` 无调用者 | 低 | `flow_controller.py` |
| FC-2 | 死代码：`report_word.py` 引用了 live 模块但从未被 `main.py` 实例化 | 低 | 已识别 |

### 13.6 预存调试打印

待清理的 `print()` → `logging.debug()`:
- `ui/calibration_tab.py:1448,1450,2114,2123,2128,2144` (6 处)

---

## 14. 升级规划建议

### 14.1 架构层面

1. **main.py 拆分 (Phase 2 Mixin):** 当前 ~2294 行的 `DataProcessorWindow` 可通过 Mixin 多继承或组合模式进一步拆分。建议评估 Mixin vs 组合模式的维护成本。

2. **配置管理统一化:** 当前配置散落在 `ai_models_config.json`、`ProjectConfig`、`AppState` 等多处。建议统一配置管理层，支持版本迁移。

3. **依赖注入容器:** 当前 `self.window()` 穿透获取主窗口属性的模式在深层嵌套中容易断裂。建议引入简单的 DI 容器或服务定位器。

### 14.2 功能层面

4. **实时数据采集:** 当前仅支持文件加载，不支持实时数据流 (串口/网络)。建议添加实时数据采集模块。

5. **数据库持久化:** 当前所有数据以 CSV/JSON 文件存储。建议引入 SQLite/PostgreSQL 做结构化数据存储和历史查询。

6. **报告模板市场:** 当前报告模板硬编码在代码中。建议支持用户自定义报告模板 (Jinja2 或类似)。

7. **批处理/自动化:** 当前所有操作需手动触发。建议添加批处理模式 (CLI) 和定时任务支持。

### 14.3 UI/UX 层面

8. **暗色主题:** 当前仅 Fusion 样式。建议添加暗色主题支持。

9. **多语言国际化:** 当前所有 UI 文本硬编码为中文。建议引入 `gettext` 国际化。

10. **可停靠面板:** 将固定标签页改为可停靠/浮动面板，提高灵活性。

### 14.4 AI 能力层面

11. **AI Function Calling 扩展:** 当前 `generate_with_tools` 已支持 Function Calling，可扩展更多工具 (数据查询、图表生成、报告导出)。

12. **诊断记录知识图谱:** 将诊断记录构建为知识图谱，支持跨项目检索和历史趋势分析。

13. **报告自动审校:** 利用多智能体系统对生成的报告进行自动质量审校。

### 14.5 工程化层面

14. **CI/CD:** 添加 GitHub Actions CI，自动运行 pytest + pyright。

15. **打包部署:** 使用 PyInstaller/Nuitka 打包为独立可执行文件。

16. **日志系统:** 将 `print()` 替换为结构化日志 (logging 模块 + 文件/控制台双输出)。

17. **性能剖析:** 对标定/报告生成等关键路径进行 profiling，识别瓶颈。

### 14.6 技术债务清理优先级

| 优先级 | 任务 | 预估工作量 |
|--------|------|-----------|
| P0 | PPT 模板缺失 crash 修复 | 0.5h |
| P0 | ConnectError 异常分类修复 | 0.5h |
| P1 | 6 处 `print()` → `logging.debug()` | 0.5h |
| P1 | 四表全空 fail-loud | 1h |
| P2 | `main.py` Mixin 拆分 | 4-8h |
| P2 | 配置管理统一化 | 4h |
| P3 | 日志系统 | 2h |
| P3 | 暗色主题 | 4h |

---

> 本文档基于 2026-07-21 的 `llama-cpp` 分支代码自动生成。  
> 生成工具: Claude Code + CodeGraph 代码智能分析
